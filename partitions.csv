# Custom partition table: the WiFi + USB-host image is larger than the default
# single-factory app partition allows. Single 1.5 MB app + NVS, no OTA.
# Name,   Type, SubType, Offset,  Size
nvs,      data, nvs,     0x9000,  0x6000
phy_init, data, phy,     0xf000,  0x1000
factory,  app,  factory, 0x10000, 0x180000
